{
    "engines": {
      "node": "18.x"
    },
    "scripts": {
      "start": "cd backend && node app.js",
      "heroku-postbuild": "cd frontend && npm install && npm run build && mv dist ../backend/"
    }
  }